/**
 * Material-UI Theme Configuration - DEPRECATED
 * Now using Tailwind CSS - see tailwind.config.js and src/index.css
 * Keeping this file for reference only
 * 
 * All theme values have been migrated to:
 * - tailwind.config.js (colors, spacing, shadows, animations)
 * - src/index.css (component styles, utilities)
 * - src/contexts/ThemeContext.tsx (dark mode toggle)
 */

// Placeholder exports to prevent import errors
export const lightTheme = {};
export const darkTheme = {};
export default lightTheme;
