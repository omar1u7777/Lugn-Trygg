# Core Flask Dependencies
Flask==3.1.2
Flask-Limiter==4.1.1
Flask-Babel==4.0.0
Werkzeug==3.1.5

# JWT Authentication
PyJWT==2.11.0
flask-jwt-extended==4.7.1

# Firebase
firebase-admin==7.1.0

# Environment & Configuration
python-dotenv==1.0.0

# Crisis Escalation Services
twilio==9.0.0
sendgrid==6.11.0

# HTTP & Requests
requests==2.31.0

# Security & Encryption
bcrypt==5.0.0
pycryptodome==3.23.0
cryptography==46.0.4
bleach==6.3.0

# Caching & Rate Limiting
redis==7.1.0

# Server
waitress==3.0.2

# AI & ML - OpenAI (pinned)
openai==1.58.1

# AI & ML - Google Cloud (pinned, minimal)
google-cloud-speech==2.28.1
google-cloud-language==2.15.0
google-cloud-storage==3.9.0

# ML & Data Science (pinned)
scikit-learn==1.8.0
numpy==1.26.4
pandas==3.0.0
joblib==1.5.3

# Deep Learning - LSTM Mood Forecasting
# [B8] tensorflow-cpu 2.18.0: verify EOL status before production deploy.
# Latest stable as of 2025-Q4 is 2.19.x; upgrade when CI passes.
tensorflow-cpu==2.18.0

# Natural Language Processing (pinned)
transformers==5.4.0
# [B8] torch is CPU-only and requires PyTorch's custom index; installed separately in Docker:
#   pip install torch==2.5.1+cpu torchvision==0.20.1+cpu --index-url https://download.pytorch.org/whl/cpu
# Pinned version: torch==2.5.1+cpu  (matches Dockerfile)

# Vector Database & Semantic Search (optional - has fallback)
# Note: package was renamed from pinecone-client to pinecone in 2024
pinecone==5.0.1
sentence-transformers==5.3.0

# Audio Analysis (optional - has fallback)
librosa==0.11.0
soundfile==0.13.1
scipy==1.17.0

# Explainable AI (optional - has fallback)
shap==0.46.0

# Payment Processing
stripe==11.3.0

# Database & Storage
google-cloud-firestore==2.23.0

# Email Services

# Data Validation
pydantic==2.12.5
pydantic-settings==2.7.0
email-validator==2.3.0
marshmallow==4.2.2

# API Documentation
apispec==6.9.0
flasgger==0.9.7.1

# Monitoring & Logging
prometheus-client==0.24.1
sentry-sdk[flask]==2.52.0
structlog==25.5.0
python-json-logger==4.0.0

# Health Data Integration
oauthlib==3.3.1
requests-oauthlib==2.0.0

# Utilities
Pillow==12.1.0
python-dateutil==2.9.0.post0
pytz==2025.2

# Two-Factor Authentication
pyotp==2.9.0
qrcode[pil]==8.0
webauthn==2.7.0
pyOpenSSL==25.3.0

# System Monitoring
psutil==7.2.2

# Task Scheduling
schedule==1.2.2

# Production Server
gunicorn==22.0.0
gevent==24.11.1
flask-socketio==5.3.7      # [B2] WebSocket biofeedback — required, not optional

# API Documentation (extended)
flask-apispec==0.11.4